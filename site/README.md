This folder holds the public site source. Key notes:

- Public pages live at the repository root: `index.md`, `about.md`, `projects.md`, `experience.md`, `certifications.md`, `contact.md`.
- Private source folders are excluded in `_config.yml` and via `.gitignore`: `about-me/`, `project-descriptions/`, `site-planning/`.
- To preview locally: use Jekyll (Ruby) or a static server. Ensure `baseurl` is respected when previewing.
- Keep the public site tight. Do not publish raw source notes, job-search reflections, or internal organizational context.
